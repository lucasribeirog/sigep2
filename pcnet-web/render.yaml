services:
  - type: web
    name: nexus-laudos-dev
    runtime: docker
    plan: free
    region: virginia

    rootDir: pcnet-web

    healthCheckPath: /health

    envVars:
      - key: NODE_ENV
        value: production

      - key: JSON_BODY_LIMIT
        value: 1mb

      - key: MAX_UPLOAD_MB
        value: "20"

      - key: AUTH_SESSION_HOURS
        value: "8"

      - key: PYTHON_BIN
        value: python3

      - key: ENABLE_SWAGGER
        value: "false"

      - key: DATABASE_URL
        sync: false

      - key: DB_POOL_MAX
        value: "5"

      - key: GEMINI_API_KEY
        sync: false