const express=require('express');const path=require('path');const multer=require('multer');const router=express.Router();const c=require('../controllers/adminController');const requireAuth=require('../middleware/requireAuth');const requireAdmin=require('../middleware/requireAdmin');const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:Number(process.env.MAX_UPLOAD_MB||20)*1024*1024,files:1}});function docx(b){return Buffer.isBuffer(b)&&b.length>=4&&b[0]===0x50&&b[1]===0x4b;}function validarDocx(req,res,next){const f=req.file;if(!f)return res.status(400).json({erro:'Selecione o template .docx.'});if(path.extname(f.originalname||'').toLowerCase()!=='.docx'||!docx(f.buffer))return res.status(415).json({erro:'O template deve ser um arquivo .docx válido.'});return next();}router.use(requireAuth,requireAdmin);router.get('/admin/dashboard',c.dashboard);router.get('/admin/formularios',c.formularios);router.get('/admin/especies',c.listarEspecies);router.post('/admin/especies',c.criarEspecie);router.patch('/admin/especies/:id',c.atualizarEspecie);router.post('/admin/especies/:id/template',upload.single('arquivo'),validarDocx,c.salvarTemplate);router.delete('/admin/especies/:id/template',c.removerTemplate);router.get('/admin/especies/:id/template',c.baixarTemplate);router.get('/admin/especies/:id/templates',c.listarVersoes);router.get('/admin/especies/:id/templates/:versao',c.baixarVersao);router.post('/admin/especies/:id/templates/:versao/restaurar',c.restaurarVersao);router.get('/admin/unidades',c.listarUnidades);router.post('/admin/unidades',c.criarUnidade);router.patch('/admin/unidades/:id',c.atualizarUnidade);router.post('/admin/unidades/importar',c.importarUnidades);module.exports=router;
