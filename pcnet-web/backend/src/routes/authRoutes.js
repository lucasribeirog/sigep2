const express=require('express');const router=express.Router();const c=require('../controllers/authController');const requireAuth=require('../middleware/requireAuth');const requireAdmin=require('../middleware/requireAdmin');
router.get('/unidades',c.listarUnidadesPublicas);router.get('/auth/bootstrap-status',c.bootstrapStatus);router.post('/register',c.registrar);router.post('/login',c.login);router.get('/auth/me',c.sessaoAtual);router.post('/auth/logout',c.logout);router.get('/admin/usuarios',requireAuth,requireAdmin,c.listarUsuarios);router.post('/admin/usuarios',requireAuth,requireAdmin,c.criarUsuario);router.patch('/admin/usuarios/:id',requireAuth,requireAdmin,c.atualizarUsuario);router.post('/admin/usuarios/:id/redefinir-senha',requireAuth,requireAdmin,c.redefinirSenha);module.exports=router;
